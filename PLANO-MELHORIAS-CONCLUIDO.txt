================================================================
PLANO DE MELHORIAS — VÉRTICE INVEST — HISTÓRICO DO QUE JÁ FOI FEITO
================================================================

Este arquivo é o ARQUIVO MORTO do PLANO-MELHORIAS.txt: guarda o que já
foi ENTREGUE [IMPLEMENTADO] ou DECIDIDO NÃO FAZER [IGNORADO], para
manter o plano principal limpo (só com o que falta).

A numeração é a MESMA do PLANO-MELHORIAS.txt — o número 3.4 aqui é o
mesmo item 3.4 de lá. Itens que ainda faltam continuam no arquivo
principal.

Atualizado em: 2026-06-17.

LEGENDA:
  [BAIXA]/[MEDIA]/[ALTA] = complexidade.
  [SONNET]/[OPUS]        = qual IA foi usada.
  [IMPLEMENTADO]         = entregue e verificado.
  [IGNORADO]             = avaliado e decidido NÃO fazer (motivo abaixo).


================================================================
1. SEGURANÇA
================================================================
1.1  Bucket de rate-limit próprio para /refresh
     Hoje o "renovar login" divide o limite com tudo. Em pico, pode
     travar usuários. Dar uma "fila própria" a ele.
     [BAIXA] [SONNET] [IMPLEMENTADO]

1.2  Validar tunables de admin com Zod
     O painel admin altera parâmetros sem validar o formato. Adicionar
     checagem para impedir valor inválido/malicioso.
     [BAIXA] [SONNET] [IMPLEMENTADO]

1.3  Rate-limit nas rotas de admin
     As rotas de configuração não têm limite de chamadas. Adicionar um
     limite (ex.: 10/hora).
     [BAIXA] [SONNET] [IMPLEMENTADO]

1.4  Token CSRF explícito nas ações sensíveis
     Hoje a proteção é indireta (cookie HttpOnly). Adicionar token
     anti-falsificação nas ações de escrita por garantia.
     [MEDIA] [OPUS] [IMPLEMENTADO]

1.5  Rotacionar segredos por precaução
     Não houve vazamento, mas trocar chaves de API/MongoDB
     periodicamente é boa higiene. Documentar o procedimento.
     [BAIXA] [SONNET] [IGNORADO]

1.6  Versionar chaves de criptografia
     A criptografia do 2FA assume sempre a mesma chave. Permitir
     "chave v1, v2..." para trocar sem quebrar usuários.
     [MEDIA] [OPUS] [IMPLEMENTADO]
     → utils/encryption.js: registro de chaves versionadas. ENCRYPTION_KEY = v1
       (compatível com tudo já cifrado); chaves extras via ENCRYPTION_KEYS
       ("v2=<hex>;v3=<hex>"); chave de cifragem ativa via ENCRYPTION_KEY_ACTIVE
       (default v1). Ciphertext novo autodescritivo "<keyId>:iv:tag:data";
       legado de 3 partes decifra com v1; texto sem ":" segue como claro.
       blindIndex FIXO em v1 (estável entre rotações → preserva unicidade do CPF).
       Novo reencrypt() (decrypt→encrypt na chave ativa) para scripts de rotação.
       _resetKeyRegistry() exposto p/ testes.
     → .env.example: documenta ENCRYPTION_KEYS e ENCRYPTION_KEY_ACTIVE + rotação.
     → tests/encryption.spec.js (novo, 12 testes): round-trip versionado, claro
       legado, ciphertext legado 3-partes, rotação v1→v2 (decifra ambos),
       reencrypt v1→v2, blind index estável pós-rotação, validações de config.

1.7  Verificar invalidação de cache no webhook de pagamento
     Quando o Mercado Pago muda o plano, confirmar que o cache do
     usuário é limpo na hora (senão ele fica com plano antigo por 5 min).
     [BAIXA] [OPUS] [IMPLEMENTADO]
     → Verificação: controllers/webhookController.js já chama invalidateUser(user._id)
       logo após user.save() no caminho 'approved' (linha ~139). Chave casa com
       authMiddleware (ambos normalizam o id via String()) — sem plano obsoleto.
     → tests/webhook_cache_invalidation.spec.js (novo, 3 testes, regressão):
       aprovado → invalidateUser('user-1') 1x; idempotência (já processado) →
       não invalida; status pending → não invalida.

1.8  Timeout em transações do banco
     Operações longas no MongoDB podem travar. Definir tempo-limite
     (ex.: 30s).
     [BAIXA] [SONNET] [IMPLEMENTADO]

1.9  Auditoria de dependências bloqueante em release
     Hoje o npm audit no CI só avisa. Fazer ele BLOQUEAR publicação se
     houver falha grave conhecida.
     [BAIXA] [SONNET] [IMPLEMENTADO]

1.10 Revisão de segurança automatizada no PR
     Rodar o /security-review em mudanças sensíveis (auth, pagamento)
     antes de juntar ao código principal.
     [MEDIA] [OPUS] [IGNORADO]
     → Requer ANTHROPIC_API_KEY como secret do repositório — não será configurado.
       Alternativa manual: rodar /code-review ou /security-review localmente
       no Claude Code antes de abrir PRs em áreas sensíveis.

1.11 Tornar o repositório do GitHub privado
     Proteger o código fechando a visibilidade do repositório.
     [MEDIA] [OPUS] [IGNORADO]
     → Decisão do dono (2026-06-15): manter o repositório PÚBLICO.
     → Verificação feita antes da decisão: (b) Actions/minutos liberado
       (CI roda ~30s–1m20s, folgado nos 2.000 min/mês do tier grátis);
       (c) links públicos/badges/raw liberado (README sem badge de CI, sem
       raw.githubusercontent, sem GitHub Pages). O único ponto que ficaria
       pendente de confirmação era (a) o deploy do Render (via GitHub App,
       sem render.yaml no repo). Como decidiu-se não fechar, nada a fazer.


================================================================
2. TESTES E QUALIDADE AUTOMATIZADA
================================================================
(MAIOR LACUNA REAL DO PROJETO no diagnóstico inicial. Backend tinha 39
testes; frontend cobria <5%. Esta seção foi 100% concluída.)

2.1  Testar o modal "Adicionar Ativo"
     É a tela mais complexa e sem teste. Cobrir compra/venda.
     [MEDIA] [SONNET] [IMPLEMENTADO]
     → assetTransaction.test.ts: +9 novos casos (makeReserveTicker x6,
       CASH saque, ticker lowercase, STOCK_US USD). Total 22 testes.
     → AddAssetModal.test.tsx (novo, 17 testes): visibilidade, BUY/SELL
       toggle, submit disabled/enabled, dropdown de venda, payload correto
       em compra e venda (qty negativa), erro data futura, erro de rede,
       CASH nome obrigatório, CASH sem reservas.

2.2  Testar a página Carteira (Wallet)
     Garantir que KPIs, abas e cálculos não quebrem no futuro.
     [MEDIA] [SONNET] [IMPLEMENTADO]
     → Wallet.test.tsx (novo, 17 testes): render, skeleton de loading, empty state,
       troca de abas (OVERVIEW/PERFORMANCE/DIVIDENDS/STATEMENT), controle de acesso
       ao Aporte Inteligente (GUEST/ESSENTIAL bloqueados, PRO libera), controle de
       acesso ao Rebalanceamento IA (non-BLACK bloqueado, BLACK abre modal,
       demo-mode dispara toast), reset de carteira (desabilitado sem ativos,
       confirm modal, chamada a resetWallet()).

2.3  Testar a página Dashboard
     Tela inicial com muitos dados; proteger contra regressões.
     [MEDIA] [SONNET] [IMPLEMENTADO]
     → Dashboard.test.tsx (novo, 7 testes): render sem erro, modal de relatório
       fechado por padrão, verificação de chamada a researchService.getLatest,
       isLoading sem crash, lógica displayDividends (dividendos reais vs projeção,
       modo privacidade mascara valor).

2.4  Testar a página Research
     Relatórios e ranking — proteger a leitura por perfil.
     [MEDIA] [SONNET] [IMPLEMENTADO]
     → Research.test.tsx (novo, 12 testes): título RESEARCH CENTER, 5 botões de
       ativo, seleção inicial BRASIL_10, controle de acesso por plano (GUEST sem
       BRASIL_10, ESSENTIAL com BRASIL_10 mas sem STOCK, PRO com STOCK, ELITE com
       STOCK_US), bypass de ADMIN (plano GUEST + role ADMIN = acesso total),
       skeleton de loading, deep link via location.state (abre AssetDetailModal
       se ticker encontrado no ranking).

2.5  Testes do fluxo de login/registro/2FA no frontend
     Simular usuário logando, errando senha, usando 2FA.
     [MEDIA] [SONNET] [IMPLEMENTADO]
     → Login.test.tsx (novo, 12 testes): render dos campos e botão, validação de
       email/senha vazios (não chama API), login bem-sucedido (chama login() e
       navega /dashboard), erro de servidor (mensagem exibida), erro sem mensagem
       (fallback genérico), mfaRequired (exibe campo de código, oculta email/senha),
       código 2FA curto (<6 dígitos = erro, API não chamada), login 2FA completo
       (mfaToken no payload, navega /dashboard), botão Voltar restaura formulário.

2.6  Testes de integração ponta-a-ponta (Playwright)
     Robô que abre o site e clica como usuário real
     (login -> carteira -> adicionar ativo).
     [ALTA] [OPUS] [IMPLEMENTADO]
     → client/playwright.config.ts (novo): testDir ./e2e, projeto chromium,
       webServer faz `npm run build && npm run preview` (porta 4173) — valida o
       MESMO bundle de produção. CI=1 ativa retries:1 e forbidOnly.
     → client/e2e/wallet-flow.spec.ts (novo, 1 jornada E2E): login (e-mail/senha)
       → /dashboard → /wallet → "Nova Transação" → compra de PETR4. Afirma o
       payload do POST /api/wallet/add (ticker/type/quantity/price/currency) e o
       toast de sucesso. Backend 100% interceptado no browser (page.route): roda
       OFFLINE, sem MongoDB nem Express — bom para CI. user.hasSeenTutorial=true
       no mock evita o modo demo (que bloquearia addAsset).
     → client/package.json: scripts test:e2e e test:e2e:install; devDependency
       @playwright/test ^1.60.
     → eslint.config.js: bloco para client/e2e/**/*.ts (parser TS, fora de src).
     → .gitignore: playwright-report/, test-results/, playwright/.cache/.
     → .github/workflows/ci.yml: novo job "e2e" (instala chromium + roda
       test:e2e; sobe playwright-report como artefato 14 dias).
     → Verificação local: 1 passed (chromium).

2.7  Completar testes do signalEngine (filtro macro)
     O motor de sinais tem parte não testada (veto em dia de crash).
     [MEDIA] [OPUS] [IMPLEMENTADO]
     → tests/signal_macro_filter.spec.js (novo, 27 testes): cobre a defesa macro
       antes não testada. isValidCorrelation — crash IBOV<-2.5% veta tudo exceto
       RSI_OVERSOLD; crash S&P<-2.5% idem p/ STOCK_US; vetos setoriais (petróleo
       <-1.5% em petroleiras, IBOV<-2.0% em VALE) e seus limiares. getMacroContext
       — detecção de crash, limiar -2.5% exato NÃO é crash, falha de rede →
       contexto neutro, índices ausentes → zeros. Auxiliares puros: _rsiUrgency,
       _grahamUrgency, calculateRSI (série curta→null, só ganhos→100, sobrevenda
       <30) e determineRiskProfile (FII/defensivo/alavancado/small-large cap/US).
       externalMarketService mockado → determinístico, sem rede/DB.

2.8  Subir cobertura do mathUtils
     A matemática do dinheiro tem só 25% das funções testadas — crítico.
     [MEDIA] [OPUS] [IMPLEMENTADO]
     → tests/math_safe_arithmetic.spec.js (novo, 21 testes): fecha a lacuna nas
       funções BASE do dinheiro que NÃO tinham teste direto — safeAdd/safeSub/
       safeMult (fundação da Regra 7 do CLAUDE.md). Cobre erro de ponto flutuante
       (0.1+0.2, 0.1*3), negativos/zeros, degenerados (null/NaN), arredondamento
       a 4 casas e truncamento de operando antes da multiplicação. Complemento de
       safeFloat/safeCurrency (2 vs 4 casas, meio-centavo) e composição encadeada
       (custo total Σ qty×preço; resultado patrimônio−custo). QUANTITY_EPSILON
       (=1e-9, abaixo de 1 satoshi). Soma-se a math_edge_cases + wallet_performance
       _math (que já cobriam Dietz/TWRR/risco/anchor/quota): mathUtils agora com
       todas as funções exportadas exercitadas.

2.9  Testes de "casos extremos" (edge cases)
     Hoje os testes cobrem o "caminho feliz". Adicionar valores
     negativos, vazios, zerados.
     [MEDIA] [SONNET] [IMPLEMENTADO]
     → kpiCalculations.test.ts: +8 casos (custo zero sem NaN, resultado negativo,
       soma de múltiplas posições, weightedRentability 0 cai para fallback,
       weightedRentability positivo preservado, sharpe/beta preservados, carteira
       vazia → AUDITED, vazia com dividendos preserva totalDividends).
     → format.test.ts: +12 casos (formatCurrency: Infinity→0, negativo, zero;
       formatPercent: Infinity→0%, negativo, sign+zero sem prefixo, sign+negativo;
       formatCompact: abaixo de 1.000 sem notação compacta, Infinity→0;
       formatQuantity: zero, maxDecimals custom, Infinity→0).

2.10 Relatório de cobertura no PR
     Mostrar automaticamente quanto do código novo está testado.
     [BAIXA] [SONNET] [IMPLEMENTADO]
     → client/package.json: novo script "test:coverage" (vitest run --coverage).
     → client/package.json: @vitest/coverage-v8 ^1.0.0 adicionado a devDependencies.
     → client/vite.config.ts: bloco coverage (provider v8, reporters text-summary/
       json/lcov, inclui src/utils, hooks, services, contexts, pages; exclui testes
       e arquivos de bootstrap).
     → .github/workflows/ci.yml: passo "Test frontend" substituído por
       "Test frontend (com cobertura)" usando npm run test:coverage; passo
       "Upload cobertura frontend" salva client/coverage/ como artefato
       retention 14 dias (visível na aba Artifacts de cada workflow run).


================================================================
3. UI / DESIGN / EXPERIÊNCIA (itens já concluídos)
================================================================
(Os itens pendentes desta seção — 3.2, 3.8 e os novos 3.11+ — seguem no
PLANO-MELHORIAS.txt.)

3.1  Feedback "salvando..." em ações lentas
     O spinner some na hora; o usuário não sabe se salvou.
     [BAIXA] [SONNET] [IMPLEMENTADO]
     → Button.tsx já possuía status (idle/loading/success/error) com "Processando..."
       + spinner; verificado em todos os formulários críticos (Login, Register,
       ForgotPassword, AddAssetModal, ContributionModal, CreateGoalModal,
       ProfileSettings, SecuritySection/MFA). Cobertura completa.

3.3  Texto maior/claro nas confirmações de exclusão
     Confirmações destrutivas têm texto pequeno. Destacar o
     "isso não pode ser desfeito".
     [BAIXA] [SONNET] [IMPLEMENTADO]
     → ConfirmModal.tsx: quando isDestructive=true, mensagem passa de text-sm/
       slate-400 para text-sm/slate-300 (mais visível); adicionado badge vermelho
       "Esta ação não pode ser desfeita" fixo; ícone destrutivo maior (h-14 w-14);
       border-t muda para blue ou red conforme contexto; botões reordenados
       (Cancelar primeiro em mobile, Confirmar primeiro em desktop).

3.4  Mensagens de erro mais humanas
     Trocar erros técnicos por frases que o usuário entende.
     [BAIXA] [SONNET] [IMPLEMENTADO]
     → client/src/utils/errorMessages.ts (novo): função friendlyError() mapeia
       status HTTP (400–504), padrões de texto (network error, timeout, email
       duplicado, senha incorreta, JWT, CPF, rate limit, not found, unauthorized).
       Aplicada em Dashboard.tsx (catch do Morning Call) e importada em Research.tsx.

3.5  Padronizar estados vazios com CTA
     Toda lista vazia com ícone + explicação + botão de ação.
     [BAIXA] [SONNET] [IMPLEMENTADO]
     → Goals.tsx: estado vazio inline convertido para o componente EmptyState
       (ícone Sparkles + descrição + botão "Nova meta") — mesma experiência de
       Wallet.tsx que já usava EmptyState. Componente EmptyState já usado em
       Wallet, Research (Análise não encontrada) e agora Goals.

3.6  Revisar responsividade no celular
     Conferir telas densas (carteira, research) em telas pequenas.
     [MEDIA] [SONNET] [IMPLEMENTADO]
     → Research.tsx: header flex-row → flex-col/md:flex-row; h1 text-4xl →
       text-2xl/md:text-4xl; ícone com variantes sm/md; ASSETS pills com w-full
       em mobile (overflow-x-auto); mb-12 → mb-8/md:mb-12.
     → Wallet.tsx: botões de ação com gap-2/md:gap-3; labels "Nova Transação",
       "Aporte Inteligente", "Rebalanceamento IA" hidden em sm (hidden sm:inline)
       — ícone visível em telas pequenas; min-w-[44px] garante área de toque
       mínima; aria-label no botão de reset.

3.7  Skeleton (esqueleto de carregamento) em todas as telas
     Onde ainda há "tela em branco", colocar o esqueleto animado.
     [BAIXA] [SONNET] [IMPLEMENTADO]
     → Goals.tsx: spinner Loader2 substituído por SkeletonKpiGrid(3) +
       SkeletonCard(2) com role="status" aria-label="Carregando metas".
     → Indicators.tsx: adicionado bloco de skeleton (SkeletonKpiGrid(7) +
       SkeletonCard x2) visível apenas no carregamento inicial (!data); conteúdo
       principal fica hidden durante esse período — evita grid com zeros piscando.

3.9  Microanimações de transição entre páginas
     Suavizar troca de telas (respeitando "reduzir movimento").
     [BAIXA] [SONNET] [IMPLEMENTADO]
     → tailwind.config.js: animação page-enter (slide-up 16px + fade, 0.35s
       cubic-bezier easing) + keyframe pageEnter.
     → client/src/components/layout/PageTransition.tsx (novo): wrapper que
       re-dispara animate-page-enter no change de pathname via useLocation;
       respeita prefers-reduced-motion (não anima se preferência ativa).
     → App.tsx: Outlet dentro de <PageTransition> — todas as páginas protegidas
       recebem a transição automaticamente.

3.10 Storybook (catálogo visual de componentes)
     Página interna que mostra botões/cards isolados — mantém consistência.
     [MEDIA] [SONNET] [IMPLEMENTADO]
     → Instalado storybook@10.4.4 + @storybook/react-vite + addon-essentials +
       addon-a11y. Scripts storybook/build-storybook adicionados ao package.json.
     → .storybook/main.ts: framework react-vite, addons [essentials, a11y].
     → .storybook/preview.tsx: importa index.css (Tailwind), backgrounds dark/card.
     → 4 stories criadas cobrindo os primitivos do design system:
       Button.stories.tsx (7 variantes: primary/outline/ghost/loading/success/
       error/disabled), EmptyState.stories.tsx (3 cenários reais), Skeleton.stories
       .tsx (7 variantes: linha/círculo/texto/card/gráfico/kpi-grid/tabela),
       ConfirmModal.stories.tsx (informativo e destrutivo).


3.13 Fotos de usuários não aparecem (landing + cadastro)  [NOVO]
     Depoimentos Ricardo/Amanda/Carlos (Landing.tsx:374-376) e os
     avatares de "Comunidade Premium" na tela de cadastro
     (AuthLayout.tsx:117-121) usavam URLs externas do Unsplash sem
     fallback — quebravam e sumiam.
     [BAIXA] [SONNET] [IMPLEMENTADO]
     → 6 fotos reais (banco randomuser.me) baixadas para
       client/public/assets/testimonials/ (ricardo.jpg, amanda.jpg,
       carlos.jpg, user1.jpg, user2.jpg, user3.jpg).
     → Landing.tsx: TestimonialCard convertido para function body com
       useState(false); ao falhar o load da imagem, exibe <div>
       circular com as iniciais do nome (ex: "RS", "AL", "CM") em azul.
       Imagens apontam para /assets/testimonials/*.jpg.
     → AuthLayout.tsx: imgs da "Comunidade Premium" apontam para
       /assets locais; onError oculta com display:none sem quebrar layout.


3.8  Modo claro (light mode) opcional
     Base já existia (ThemeContext + CSS vars + toggle no Header/Profile).
     Concluído com 3.12 (refino da landing) + 3.15 (claro vira padrão).
     [ALTA] [OPUS] [IMPLEMENTADO]
     → Arquitetura de tokens em index.css (:root dark / [data-theme=light])
       já remapeia text-white, text-slate-*, chips coloridos, bordas e
       surfaces. Light agora é o padrão e a landing foi auditada (ver 3.15/3.12).


3.11 Aporte Inteligente corta em telas menores  [NOVO]
     No notebook o modal de Aporte Inteligente cortava o conteúdo sem
     barra de rolagem.
     [BAIXA] [SONNET] [IMPLEMENTADO]
     → SmartContributionModal.tsx:152: o div de conteúdo (<div className="p-6">)
       ganhou "flex-1 min-h-0 overflow-y-auto custom-scrollbar", copiando o
       padrão que já funciona em AddAssetModal.tsx:691. O painel já tinha
       max-h-[90vh] flex flex-col, então o corpo agora rola entre header e
       footer fixos. Verificado em ~768px de altura.


3.12 Refinar o tema claro na landing page  [NOVO]
     A landing tinha cores presas no escuro que não respondiam ao tema claro.
     [MEDIA] [SONNET] [IMPLEMENTADO]
     → Auditoria seção a seção: features, testimonials, footer, ticker e
       chips já adaptavam via tokens (bg-deep/bg-base) + overrides
       [data-theme=light] do index.css (text-white→escuro, slate→claro).
     → Aresta real corrigida: os 2 CTAs com bg-white sólido (navbar
       "Começar Agora" e CTA final "Criar Conta Gratuitamente") ficavam
       brancos invisíveis sobre o fundo claro — bg-white sólido não é
       remapeado pelos overrides. Agora usam useTheme(): no claro viram
       bg-blue-600 text-white (CTA azul de marca); no escuro mantêm o
       bg-white original. Nenhuma mudança no visual escuro.
     → Refino (2ª passada, feedback do usuário): títulos sem classe de cor
       (herança não entregava o override) ficavam brancos sobre fundo claro.
       Adicionado text-white explícito em "O Fim da" (hero), "Sua jornada
       para a elite", "Quem usa, recomenda" e "Perguntas Frequentes" → o
       override [data-theme=light] .text-white→#0f172a age direto no elemento
       (escuro no claro, branco no escuro).
     → Sombra dos cards: testimonials/FAQ/results eram bg-deep/bg-base
       rounded-xl → fora da auto-elevação do index.css (que só pega
       base/card/panel + rounded-2xl/3xl). Migrados p/ bg-card/bg-base
       rounded-2xl → ganham a sombra clara automática e, no escuro, ficam
       levemente acima da seção (card lighter than section).
     → PerformanceCard (barras "Acumulado 12m"): competidores usavam
       bg-slate-700, que no claro cai p/ ~#e2e8f0 e sumia sobre a track
       quase branca. Agora a cor do competidor é theme-aware (bg-slate-400
       sólido no claro, visível; bg-slate-700 no escuro).


3.14 Tirar o ícone de cérebro do "Criar Conta Grátis"  [NOVO]
     [BAIXA] [SONNET] [IMPLEMENTADO]
     → Landing.tsx: <BrainCircuit size={18}/> do botão do hero trocado por
       <Bot size={18}/> (combina com o tema de IA). Import Bot adicionado;
       BrainCircuit mantido (ainda usado no card de features).


3.15 Tema claro como padrão do site  [NOVO]
     [BAIXA] [SONNET] [IMPLEMENTADO]
     → ThemeContext.tsx: default sem preferência salva agora é 'light'
       (createContext e useState: stored === 'dark' ? 'dark' : 'light').
       Quem já escolheu um tema mantém a escolha (localStorage 'vertice-theme').
     → index.html: script anti-FOUC aplica data-theme="light" por padrão e
       só mantém escuro se localStorage === 'dark' (inclui fallback no catch).
     → Feito depois de 3.12 para não expor arestas do tema claro.


3.16 Trocar o badge "Segurança Bancária" na landing  [NOVO]
     [BAIXA] [SONNET] [IMPLEMENTADO]
     → Landing.tsx (hero): badge "Segurança Bancária" → "Cancele quando
       quiser" (claim honesto, alinhado ao CTA do rodapé).
     → Bônus (feedback): removido o CNPJ placeholder "00.000.000/0001-00"
       do rodapé da landing.


3.18 "Recursos Ativos" com descrições corretas  [NOVO]
     [BAIXA] [SONNET] [IMPLEMENTADO]
     → SubscriptionCard.tsx: planDetails.features reescritas com as vantagens
       REAIS por tier, derivadas de PLAN_ACCESS/PLAN_EXCLUSIVE
       (constants/subscription + Pricing). Cada plano herda o anterior
       ("Tudo do Essential/Pro/Elite") e lista só o que agrega. Layout
       (grid 2 col) intacto.


3.19 "Renovação em" + preço (corrigir desproporção)  [NOVO]
     [BAIXA] [SONNET] [IMPLEMENTADO]
     → SubscriptionCard.tsx: removido o badge solto "Renovação em {dia}" do
       lado do status; validade + renovação agrupadas num único bloco coeso
       (md:min-w-[200px]) com linhas "Válido até" / "Renova dia" alinhadas.
     → Feedback do usuário: o preço grande ("R$ X/mês") foi REMOVIDO desse
       bloco (intimidava); o preço fica no /pricing. Plano gratuito mostra
       "Plano Gratuito / Acesso vitalício".
     → Status virou chip colorido (ACTIVE=verde c/ pulse, TRIAL=azul,
       PAST_DUE=amarelo, CANCELED=vermelho) — classes de chip já têm override
       de tema claro, bonito nos dois temas. Idem em ProfileIdentity.


3.20 Trocar o banner do perfil  [NOVO]
     [MEDIA] [SONNET] [IMPLEMENTADO]
     → Backend: User.bannerColor (enum de presets), updateProfile valida
       contra allowlist ('' faz $unset → volta ao padrão do plano),
       sanitizeUser e getSubscriptionStatus passam a expor o campo.
     → Frontend: ProfileIdentity com seletor de 6 presets de gradiente
       (ocean/emerald/royal/sunset/gold/graphite) + "Padrão do plano".
       Aplica via authService.updateProfile + refreshProfile. Gradiente por
       plano mantido como fallback. Sem upload (3.17 fora deste lote).
     → Feedback: botão da paleta usa text-[#fff] literal (o banner é sempre
       escuro e o override de tema claro inverteria text-white → some).


3.22 Ícone do método de pagamento (Banco/Cartão)  [NOVO]
     [BAIXA] [SONNET] [IMPLEMENTADO]
     → getSubscriptionStatus expõe current.paymentMethod (= método da última
       Transaction: CREDIT_CARD|PIX|CRYPTO). AuthContext.User ganhou o campo.
     → SubscriptionCard mapeia método → ícone+rótulo (CreditCard/Cartão,
       QrCode/Pix, Bitcoin/Cripto); sem transação cai no CreditCard +
       "Mercado Pago (Pré-pago)". Só a referência do método vai ao BD.


3.17 Upload de foto de perfil  [NOVO]
     [MEDIA] [OPUS] [IMPLEMENTADO]
     → Decisão de armazenamento: data-URL pequena no Mongo (sem storage
       externo). A imagem é redimensionada/comprimida NO CLIENTE para um
       quadrado 256×256 (crop "cover" via canvas) e exportada em WebP 0.82
       (fallback JPEG) — payload típico ~5-20KB, bem abaixo do limite de
       1mb do express.json.
     → Backend: User.avatar (String). Endpoints PUT /api/me/avatar e
       DELETE /api/me/avatar (authController.updateAvatar/removeAvatar).
       Validação defensiva no servidor (nunca confia no cliente): regex de
       data-URL image/(png|jpeg|webp);base64 + teto de tamanho (400K chars
       → 413). Rate limiter dedicado avatarUploadLimiter (20/15min) por
       conta do payload maior. Zod avatarSchema na rota. sanitizeUser passa
       a expor avatar; audit log AVATAR_UPDATED/REMOVED.
     → Frontend: ProfileIdentity com <input type=file> oculto, resize no
       cliente (resizeToDataUrl), preview no círculo do avatar (img cobre as
       iniciais), overlay de câmera/spinner no hover, botões "Adicionar/
       Trocar Foto" + remover. authService.updateAvatar/removeAvatar
       atualizam o localStorage; refreshProfile reflete no contexto.

3.21 Novos campos em "Informações Pessoais"  [NOVO]
     [MEDIA] [OPUS] [IMPLEMENTADO]
     → Decisão LGPD: birthDate e salary são CIFRADOS em repouso
       (AES-256-GCM via utils/encryption), mesma postura do CPF — guardados
       como ciphertext "keyId:iv:tag:data" e devolvidos em claro só ao
       titular (sanitizeUser/exportData). Endereço e corretora ficam em
       claro (PII de baixo grau, como phone/occupation).
     → Backend: User ganhou brokerage, cep, street, neighborhood, city,
       state, birthDate, salary. updateProfile valida cada um (corretora
       texto livre ≤80; data real não-futura e idade ≤120; salário número
       ≥0; '' → $unset). Zod updateProfileSchema na rota PUT /me (antes sem
       schema). exportData decifra birthDate/salary.
     → Frontend: ProfileSettings com select de corretora (lista conhecida +
       "Outra" → campo livre), CEP com autopreenchimento via ViaCEP
       (gratuito, client-side, CORS liberado) preenchendo logradouro/bairro/
       cidade/UF, Data de Nascimento (type=date) e Salário (type=number).
       ProfileIdentity mostra cidade/UF no lugar de "Brasil" quando houver.


--- Ajustes de UX por feedback (mesmo lote) ---
   → Zona de Perigo (Profile.tsx): os dois gatilhos ("Desativar Conta" e
     "Excluir Conta Permanentemente") agora ficam numa linha responsiva
     (flex-col no mobile, flex-row no desktop, flex-1). Ao abrir um fluxo a
     linha some e o formulário ocupa a largura toda.
   → Ícones nos títulos de seção do perfil para consistência com
     "Segurança & Acesso" (Lock): Informações Pessoais (User), Aparência
     (Palette), Dados e Privacidade (Database), Zona de Perigo (AlertTriangle).


================================================================
5. PERFORMANCE (itens já concluídos)
================================================================
5.2  useMemo na filtragem da AssetTable
     [BAIXA] [SONNET] [IMPLEMENTADO]
     → AssetTable.tsx: o reduce de agrupamento (groupedItems) e a ordenação
       (orderedGroups) passaram a useMemo([items]). Antes recalculavam a cada
       render (toggle de grupo, abrir modal de aporte/rebalanceamento etc.).


5.5  Analisador de tamanho do bundle
     [BAIXA] [SONNET] [IMPLEMENTADO]
     → rollup-plugin-visualizer (devDep) em vite.config.ts, ativado só por
       ANALYZE=true (gera dist/stats.html — treemap com gzip/brotli). Script
       `npm run analyze` (client) via cross-env (cross-plataforma). Build
       normal não é afetado.


5.6  Imagens em WebP/AVIF
     [BAIXA] [SONNET] [IMPLEMENTADO]
     → vite-plugin-image-optimizer (+ sharp/svgo) comprime PNG/JPG/SVG do
       bundle e do /public no build. Ganhos reais medidos: og-image.png
       807KB→71KB (-92%); imagens de marketing Feed/Story ~1,4MB→~300-490KB
       (-70 a -81%). Imagens que cresceriam são puladas (mantém original).
       Suporta saída webp/avif p/ assets futuros importados pelo bundler.
       Só roda em `vite build` (não afeta dev/HMR).
     → Nota: og-image mantém PNG (compatibilidade de preview social/OG); os
       depoimentos já eram <6KB (ganho marginal).


5.7  TTL (expiração) em coleções que só crescem
     [BAIXA] [SONNET] [IMPLEMENTADO]
     → MarketAnalysis.js: índice TTL parcial em createdAt (90 dias) com
       partialFilterExpression { isRankingPublished: false } — expira só
       rascunhos/runs não publicados; relatórios publicados (histórico
       canônico de ranking/accuracy) são preservados. DiscardLog (7d) e
       QuantSignal (30d) já tinham TTL.


5.9  Lighthouse CI de performance
     [MEDIA] [SONNET] [IMPLEMENTADO]
     → .github/workflows/lighthouse.yml: builda o client e roda
       treosh/lighthouse-ci-action@v12 a cada PR. .lighthouserc.json serve
       client/dist (staticDistDir, preset desktop, 3 runs) e usa asserts em
       modo "warn" (perf 0.8 / a11y 0.9 / best-practices 0.9 / seo 0.9) —
       acompanha a nota sem bloquear o merge; relatório em link público temp.


5.10 Debounce nas buscas de ativos
     [BAIXA] [SONNET] [IMPLEMENTADO/VERIFICADO]
     → Já estava implementado nos hooks extraídos do AddAssetModal:
       useAssetSearch (autocomplete de ticker, debounce 300ms + clear no
       teardown) e usePriceFetch (cotação automática, debounce 600ms + cache
       de sessão). Nenhuma outra busca de ativo dispara API por tecla
       (Calculator/Research usam <select> ou filtro client-side). Verificado.


5.3  Promise.allSettled nas cotações  (entregue 2026-06-17)
     Se 1 cotação falha, toda a busca falha. Trocar para "cada uma por si".
     [MEDIA] [OPUS] [IMPLEMENTADO]
     → Núcleo: novo marketDataService.getMarketDataMap(tickers) resolve N
       tickers em ≤2 queries e cada ticker é INDEPENDENTE — um símbolo sem
       dado vira { price: 0 } e nunca derruba os demais (substitui o
       Promise.all(map(getMarketDataByTicker)) que poderia abortar em bloco).
       Falha total de DB ainda devolve uma chave por ticker (price 0).
     → walletController.getWalletData: o lote de leituras passou a usar
       Promise.allSettled — se o cálculo de dividendos (ou macro/snapshots)
       falhar, a carteira renderiza com degradação graciosa (totalDividends=0,
       config null → defaults) em vez de responder 500.
     → tests/market_data_service.spec.js: +6 testes de getMarketDataMap
       (chave original vs normalizada, fallback de histórico em 1 query,
       ticker desconhecido isolado, resiliência a falha total de DB).

5.4  Paralelizar consultas em getWalletData  (entregue 2026-06-17)
     Algumas consultas ao banco são sequenciais; rodar juntas.
     [MEDIA] [OPUS] [IMPLEMENTADO]
     → walletController.getWalletData: (1) userPrefs (User.findById) +
       userAssets (UserAsset.find) agora em Promise.all no topo — antes
       sequenciais. (2) Quatro leituras independentes — cotações em lote,
       macro (SystemConfig), dividendos (calculateUserDividends) e os 30
       snapshots do TWRR/Sharpe — unificadas num único Promise.allSettled;
       o WalletSnapshot.find que vivia ~150 linhas abaixo foi hasteado para
       esse lote (mesma query, agora paralela). txs do Modified Dietz segue
       depois pois depende do snapshot-âncora.
     → calculateLiveKPIS: dividendos + macro + cotações em lote (Promise.all).

5.8  Revisar consultas N+1 restantes  (entregue 2026-06-17)
     Encontrar onde o código faz "1 consulta por item" em vez de 1 só.
     [MEDIA] [OPUS] [IMPLEMENTADO]
     → Padrão N+1 eliminado em 4 caminhos de request que faziam um
       MarketAsset.findOne por ativo dentro de loop (1 refresh em lote + N
       findOne = N+1): getWalletData e calculateLiveKPIS (walletController),
       getLiveWalletEquity (goalsController) e computeWalletValuation
       (rebalanceService). Todos passaram a usar getMarketDataMap — UMA query
       em MarketAsset (+ 1 fallback em AssetHistory só p/ quem não tem preço),
       com lookup O(1) por Map. scheduler(job diário em background) e scripts
       offline foram deixados como estão (fora do caminho de request).


================================================================
6. BACKEND E ARQUITETURA (itens já concluídos)
================================================================
(Seção 6 totalmente concluída — 6.1 a 6.10.)

6.1  Tratador de erros estruturado  (entregue 2026-06-17)
     Hoje o erro só devolve a mensagem. Padronizar com
     código + mensagem + detalhe.
     [MEDIA] [OPUS] [IMPLEMENTADO]
     → server/utils/AppError.js (novo): classe de erro com { status, code,
       details }. code default derivado do status (STATUS_CODE map). Atalhos
       AppError.badRequest/unauthorized/forbidden/notFound/conflict.
     → server/middleware/errorHandler.js (novo): substitui o handler inline de
       app.js. buildErrorResponse(err, requestId) é PURO (testável) e devolve
       { status, body }. Body padronizado:
         { message, error: { code, message, details?, requestId? } }
       message no topo mantém COMPAT (todo o frontend lê data.message). Infere
       status/code de erros nativos: Mongoose ValidationError (400 + detalhes
       por campo), CastError (400 INVALID_ID), duplicate key 11000 (409
       DUPLICATE_KEY + campos), JWT (401). Honra o legado err.httpStatus
       (txError) e err.status/statusCode. requestId vem do correlation id (D12).
       5xx loga em error (com stack), 4xx em warn.
     → app.js: importa e usa errorHandler no lugar do handler antigo.
     → walletController.addAssetTransaction: throws de validação migrados de
       new Error(...) (que viravam 500) para AppError.badRequest(...) (400).
     → tests/error_handler.spec.js (novo, 8 testes): AppError, httpStatus legado,
       duplicate key, ValidationError com detalhes, JWT, fallback 500, mensagem
       padrão, middleware escrevendo status+body. Adicionado ao test:ci.

6.2  Contador de segurança na recursão de getWalletData  (entregue 2026-06-17)
     A função se chama de novo após "auto-curar"; adicionar limite para
     nunca entrar em loop.
     [BAIXA] [OPUS] [IMPLEMENTADO]
     → walletController.getWalletData ganhou um 4º parâmetro interno _depth=0 e
       a constante MAX_WALLET_HEAL_DEPTH=1. Após o auto-heal reconstruir
       posições, só recursa se _depth < limite; ao atingir o teto loga warn e
       renderiza o estado atual em vez de recursar — impossível entrar em loop
       infinito mesmo que o recálculo reporte sucesso sem estabilizar.

6.3  Quebrar getWalletData (~300 linhas) em partes  (entregue 2026-06-17)
     Função gigante e difícil de testar; dividir em funções menores.
     [MEDIA] [OPUS] [IMPLEMENTADO]
     → walletController: getWalletData passou de ~300 linhas para um
       orquestrador enxuto + 5 helpers de responsabilidade única, com a MESMA
       aritmética financeira (ordem de safeAdd preservada):
         loadWalletState(userId) — prefs+holdings (Promise.all), targets,
           active/closed; autoHealPositions(userId) — reconstrói e devolve os
           curados ou null; buildEmptyWalletResponse(targets) — carteira vazia;
         fetchWalletMarketContext(userId, liveTickers) — o lote Promise.allSettled
           (cotações/macro/dividendos/snapshots); processWalletAsset(asset, ctx)
           — resolve preço/variação de 1 ativo e devolve o card + contribuições
           p/ os totais; computeWalletMetrics({...}) — TWRR live + Sharpe.
       Cada helper é pequeno e testável isoladamente. Validado: 245→253 testes
       verdes (suíte de carteira/financeira inclusa) sem mudança de saída.

6.4  Quebrar rebuildUserHistory (~200 linhas)  (entregue 2026-06-17)
     Mesma ideia, na função que reconstrói o histórico da carteira.
     [MEDIA] [OPUS] [IMPLEMENTADO]
     → financialService: rebuildUserHistory (~346 linhas) virou um orquestrador
       enxuto + 7 helpers de responsabilidade única, com a MESMA aritmética
       financeira (ordem de operações e fatores preservados):
         _loadUsdRateResolver(currentUsdRate) — carrega USD/BRL e devolve o
           resolvedor de taxa por data (busca binária <= alvo);
         _loadPriceCacheMap(tickers, metaMap) — cache de preços por ticker, com
           fallback ao externalMarketService e upsert em AssetHistory;
         _loadDividendDateMap(tickers) — proventos indexados por data;
         _loadCdiFactors(startDate, today, cdi) — Map de fatores SELIC do banco
           (também flag de dia útil) + fallback prefixado por ano;
         _applyDayTransactions(ctx) — aplica as movimentações do dia (muta
           portfolio/fixedIncomeState) e devolve txIndex + fluxo ajustado;
         _accrueDailyFixedIncome(ctx) — juros da renda fixa só em dia útil;
         _markPortfolioToMarket(ctx) — marcação a mercado → patrimônio
           nominal/ajustado, investido e hasPosition;
         _persistSnapshots(userId, snaps) — troca transacional em lotes de 5000.
       O loop diário agora lê em 5 passos numerados. Removidas 2 variáveis mortas
       (dayFlowNominal e previousEquityNominal eram acumuladas e nunca lidas).
       Validado: 253 testes verdes + node --check sem mudança de saída.

6.5  Tornar explícitas as "falhas silenciosas"  (entregue 2026-06-17)
     Alguns serviços engolem erro sem avisar. Ou avisa, ou comenta
     claramente que é proposital.
     [BAIXA] [OPUS] [IMPLEMENTADO]
     → Eliminados os catch/`.catch()` vazios que sumiam com o erro. Agora cada um
       OU loga OU tem comentário explicando que o silêncio é proposital:
         walletController: os 3 rebuildUserHistory pós-commit (add/remove ativo,
           remove transação) e os 2 background ops (refreshQuotesBatch,
           syncDividends) passaram a logar warn com contexto (user/ticker) — a
           transação principal já commitou, então é best-effort, mas visível.
         rebalanceService.refreshQuotesBatch e configService.getTunablesSync
           (refresh de tunables em background) idem → logger.warn.
         authController.logAudit e financialService (fallback externo de
           histórico) → comentário "proposital" + logger.debug (não podem
           derrubar auth/rebuild; ruído mínimo).

6.6  Logs estruturados (em vez de texto solto)  (entregue 2026-06-17)
     Padronizar logs em formato de dados (facilita busca).
     [MEDIA] [SONNET] [IMPLEMENTADO]
     → config/logger.js: o Winston ganhou logs estruturados sem quebrar os ~500
       call sites antigos (que passam só string):
         • injectRequestId promove o correlation id (D12) a CAMPO (info.requestId),
           não só prefixo de texto.
         • Call sites podem passar metadados no 2º arg: logger.info(msg, { source,
           count, ... }). No console/arquivos de texto vira " | key=value"; no
           novo transport JSON vira campo de 1ª classe.
         • Novo transport combined.json.log (winston.format.json) — 1 linha/evento,
           pronto para jq/Loki/Datadog. Mantidos error.log e combined.log legíveis.
       → Padrão já adotado nos logs de maior valor: scraper Fundamentus
         (source/kind/count/schemaVersion/status) e rebuildUserHistory
         (durationSec/snapshots/userId). Demais logs migram incrementalmente.
     → tests/logger_structured.spec.js (3 testes): transport JSON presente,
       aceita metadados, compat com string pura. Adicionado ao test:ci.

6.7  Versionar os "raspadores" (scraping) frágeis  (entregue 2026-06-17)
     Fundamentus/FII quebram se o site mudar o HTML. Isolar e versionar.
     [MEDIA] [OPUS] [IMPLEMENTADO]
     → config/scraperSchemas.js (novo): isola e VERSIONA o conhecimento frágil de
       layout do Fundamentus (ordem das colunas). FUNDAMENTUS_STOCKS_LAYOUT e
       FUNDAMENTUS_FIIS_LAYOUT trazem { version, source, table, columns (nome→
       índice), headerSignature }. validateFundamentusLayout($, layout) lê o
       cabeçalho real e devolve { ok, version, mismatches }.
     → fundamentusService: getStocksMap/getFIIsMap não usam mais índices mágicos
       ($(tds[18])) e sim nomes do schema ($(tds[col.patrimLiq])). Antes de raspar,
       validam o layout e logam warn estruturado se a estrutura divergir — troca a
       falha SILENCIOSA (dados zerados) por um alerta acionável. Bump de `version`
       quando o site mudar.
     → tests/scraper_schema.spec.js (5 testes): cabeçalho válido (tolera acento/
       caixa), colunas-chave fora do lugar e tabela ausente. Adicionado ao test:ci.

6.8  Completar a documentação Swagger da API  (entregue 2026-06-17)
     Várias rotas não estão documentadas no /api/docs. Anotar todas.
     [MEDIA] [SONNET] [IMPLEMENTADO]
     → config/swagger.js: o paths passou de ~14 para ~70 endpoints, cobrindo TODAS
       as rotas reais (conferidas em routes/*.js): auth completo (forgot/reset
       password, avatar, change-password, deactivate, export/delete LGPD), MFA,
       wallet (history/search/dividends/cashflow/rebalance/transactions/admin),
       research leitura + bloco "Research (Admin)", market (landing/logo/price/
       status), subscription (return/confirm/sync/check-access/register-usage),
       goals (CRUD + contributions), academy (cursos/lições/quiz/certificado),
       notifications e webhooks. Novas tags por área; rotas públicas (sem JWT)
       marcadas com security: [] (login, forgot-password, market/landing,
       academy/courses, webhooks…). Prefixos batem com os mounts do app.js.
     → tests/swagger.spec.js: +3 testes (cobertura de todas as áreas, tags por
       área, security:[] nas públicas). 8 testes verdes.

6.9  Circuit breaker na conexão do MongoDB  (entregue 2026-06-17)
     Se o banco fica lento, os pedidos acumulam. Adicionar "disjuntor".
     [MEDIA] [OPUS] [IMPLEMENTADO]
     → middleware/mongoCircuitBreaker.js (novo): "disjuntor" que faz FAIL-FAST.
       Quando a conexão não está em `connected` (readyState !== 1) OU o circuito
       está aberto, as rotas de dados respondem 503 + Retry-After NA HORA, em vez
       de enfileirar até o serverSelectionTimeout (30s) e esgotar pool/memória.
       Reaproveita o CircuitBreaker de utils/resilience (threshold 3, cooldown
       15s) ligado aos eventos da conexão (disconnected/error → falha;
       connected/reconnected → fecha). Decisão isolada em shouldRejectRequest()
       (pura/testável).
     → config/db.js: attachMongoBreaker() ligado ANTES do connect.
     → app.js: app.use('/api', mongoCircuitBreaker) depois do /api/health (que
       precisa responder com o banco fora) e antes das rotas; /api/health agora
       expõe dbBreaker (CLOSED|OPEN|HALF_OPEN).
     → tests/mongo_circuit_breaker.spec.js (7 testes): decisão pura, middleware
       (next vs 503+Retry-After) e abertura por limiar de falhas. Adicionado ao
       test:ci.

6.10 Normalizar tratamento de datas  (entregue 2026-06-17)
     Há mistura de Date/string/timezone. Centralizar num utilitário único.
     [MEDIA] [OPUS] [IMPLEMENTADO]
     → utils/dateUtils.js: centraliza os helpers antes duplicados/ad-hoc, com a
       CONVENÇÃO de fuso documentada:
         toDateKey(date) — chave `YYYY-MM-DD` (base UTC, via toISOString; null em
           entrada inválida) p/ comparar/agrupar/indexar por dia;
         startOfDay(date) — meia-noite LOCAL (novo Date);
         dateKeyToUtcDate(date) — chave → Date de meia-noite UTC estável.
     → Adoção nos caminhos críticos, SEM mudar semântica (helpers replicam o
       comportamento antigo):
         financialService.toDateKey/normalizeDate agora delegam ao util (removida
           a 2ª cópia da lógica); todos os `this.toDateKey` internos preservados;
         walletController: 7 sites de `x.toISOString().split('T')[0]` → toDateKey
           e setHours(0,0,0,0) → startOfDay;
         schedulerService: `new Date(x.toISOString().split('T')[0]+'T00:00:00Z')`
           → dateKeyToUtcDate(x).
       Demais serviços (externalMarketService etc.) migram incrementalmente.
     → tests/date_utils.spec.js (6 testes): equivalência com o padrão antigo,
       null-safety, startOfDay imutável, dateKeyToUtcDate. Adicionado ao test:ci.


================================================================
7. ENGINES FINANCEIRAS E FUNCIONALIDADES
================================================================
7.2  Detecção de divergências nos sinais  (entregue 2026-06-17)
     Identificar quando preço e indicador "discordam" (sinal de virada).
     [ALTA] [OPUS] [IMPLEMENTADO]
     → services/engines/signalEngine.js: novo conjunto de funções PURAS
       (testáveis, sem rede/DB):
         _rsiSeries(closes, period) — série de RSI "dia a dia" alinhada ao
           histórico (mais recente→antigo); out[i] = RSI sobre closes[i..i+period].
         detectBullishDivergence(closes, period, opts) — divergência ALTISTA:
           o PREÇO faz fundo mais baixo enquanto o RSI faz fundo mais ALTO
           (momentum de baixa enfraquece) → possível virada. Detecta pivôs de
           baixa (mínimos locais, wing=2) dentro do lookback (40), exige
           separação mínima (3 pregões), fundo recente fresco (≤6 pregões) e
           preço já reagindo acima do fundo, com RSI recente em zona fraca (<45).
         _divergenceUrgency(rsi) — urgência por força da sobrevenda no fundo
           recente (CRITICAL <25, HIGH <35, MEDIUM demais).
     → runScanner CHECK 3: novo sinal BULLISH_DIVERGENCE, com as MESMAS travas
       de qualidade do RSI (margem líquida > -5, confirmação de volume, bloqueio
       de downtrend severo < 85% da SMA200 e filtro de correlação macro).
     → models/QuantSignal.js: enum de `type` ganhou 'BULLISH_DIVERGENCE'.
     → Frontend: Radar.tsx (rótulo "Divergência Altista" + tag de RSI),
       AiRadar.tsx (badge de RSI) e useDashboardData.ts (tipos + score =
       100 - RSI, igual ao RSI_OVERSOLD).
     → tests/signal_divergence_volume.spec.js: cobre detecção (positivo),
       série curta/entrada inválida/monotônica/sem lower-low (negativos) e
       _rsiSeries. Adicionado à suíte (404 testes verdes no server).

7.3  Filtro por volume nos sinais  (entregue 2026-06-17)
     Considerar volume negociado para validar o sinal.
     [MEDIA] [OPUS] [IMPLEMENTADO]
     → ANTES o histórico não persistia volume (AssetHistory só guardava
       date/close/adjClose), então a "confirmação de volume" do runScanner era
       um no-op silencioso (volume sempre undefined → não bloqueava nada).
       Corrigido na raiz:
         models/AssetHistory.js: subdoc `history` ganhou o campo `volume`.
         externalMarketService.getFullHistory: passou a mapear `volume`
           (day.volume) vindo do Yahoo, antes descartado.
     → services/engines/signalEngine.js: extraído _volumeStats(history,
       multiplier=1.2, lookback=20) — função PURA que devolve
       { hasData, currentVolume, avgVolume, ratio, confirmed }. Exige volume
       atual ≥ 1.2× a média dos últimos 20 pregões; quando NÃO há dados
       suficientes (histórico legado), degrada com segurança (confirmed=true,
       não bloqueia). Aplicado aos sinais RSI_OVERSOLD e BULLISH_DIVERGENCE.
     → models/QuantSignal.js: novo campo `volumeRatio` (razão no momento do
       sinal; null quando sem dados) gravado nos upserts.
     → tests/signal_divergence_volume.spec.js: cobre confirmação, não-confirmação,
       ausência de dados (não bloqueia) e descarte de volumes inválidos/zerados.

(7.1, 7.4–7.6 e 7.9 seguem pendentes no PLANO-MELHORIAS.txt.)


================================================================
FIM DO HISTÓRICO — 55 itens arquivados (1.1–1.10, 2.1–2.10,
3.1/3.3–3.22 exceto 3.2,
5.2/5.3/5.4/5.5/5.6/5.7/5.8/5.9/5.10, 6.1/6.2/6.3, 7.2/7.3)
================================================================
